# Workspace-level clippy tuning.
# Lint levels live in Cargo.toml ([workspace.lints]).
# Only keep keys for lints that are not fully allowed/denied at the workspace level.
too-many-arguments-threshold = 8
