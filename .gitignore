/result
.aider*
.claude/
.direnv/
.envrc

target/
.tmp/
/nvim/artifacts/

# SOPS plaintext artifacts (encrypted secrets are safe to commit)
/secrets/*_unencrypted*
/secrets/*.dec
/secrets/*.decrypted
/secrets/*.clear
/secrets/*.plain
