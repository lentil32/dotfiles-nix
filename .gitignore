/result
.aider*
.claude/

target/
/.tmp/
/nvim/artifacts/

# SOPS plaintext artifacts (encrypted secrets are safe to commit)
/secrets/*_unencrypted*
/secrets/*.dec
/secrets/*.decrypted
/secrets/*.clear
/secrets/*.plain
